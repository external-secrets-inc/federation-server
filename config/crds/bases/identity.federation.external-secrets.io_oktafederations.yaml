apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    external-secrets.io/component: controller
  name: oktafederations.identity.federation.external-secrets.io
spec:
  group: identity.federation.external-secrets.io
  names:
    categories:
    - external-secrets
    - external-secrets-federation
    kind: OktaFederation
    listKind: OktaFederationList
    plural: oktafederations
    singular: oktafederation
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OktaFederation represents an Okta federation configuration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OktaFederationSpec defines the specification for Okta federation.
            properties:
              authorizationServerId:
                default: default
                description: |-
                  AuthorizationServerID is the OAuth2 Authorization Server ID (e.g., "default" or custom auth server ID)
                  Defaults to "default" if not specified
                type: string
              domain:
                description: Domain is the Okta organization domain (e.g., "https://dev-12345.okta.com"
                  or custom domain)
                type: string
              managementAPI:
                description: |-
                  ManagementAPI credentials for checking if Okta applications still exist
                  This is optional - if not provided, identity existence checking will be skipped
                properties:
                  apiTokenSecretRef:
                    description: APITokenSecretRef references a secret containing
                      the Okta API token
                    properties:
                      key:
                        description: Key in the secret
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                      namespace:
                        description: Namespace of the secret
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - apiTokenSecretRef
                type: object
            required:
            - domain
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
