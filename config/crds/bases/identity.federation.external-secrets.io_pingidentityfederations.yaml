apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    external-secrets.io/component: controller
  name: pingidentityfederations.identity.federation.external-secrets.io
spec:
  group: identity.federation.external-secrets.io
  names:
    categories:
    - external-secrets
    - external-secrets-federation
    kind: PingIdentityFederation
    listKind: PingIdentityFederationList
    plural: pingidentityfederations
    singular: pingidentityfederation
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PingIdentityFederation represents a PingIdentity federation configuration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PingIdentityFederationSpec defines the specification for
              PingIdentity federation.
            properties:
              environmentId:
                description: EnvironmentID is the PingOne environment ID (UUID)
                type: string
              managementAPI:
                description: |-
                  ManagementAPI credentials for checking if PingOne applications still exist
                  This is optional - if not provided, identity existence checking will be skipped
                  Requires a Worker application with appropriate permissions
                properties:
                  clientIdSecretRef:
                    description: ClientIDSecretRef references a secret containing
                      the Worker app client ID
                    properties:
                      key:
                        description: Key in the secret
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                      namespace:
                        description: Namespace of the secret
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  clientSecretRef:
                    description: ClientSecretRef references a secret containing the
                      Worker app client secret
                    properties:
                      key:
                        description: Key in the secret
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                      namespace:
                        description: Namespace of the secret
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - clientIdSecretRef
                - clientSecretRef
                type: object
              region:
                description: Region is the PingOne region (e.g., "com", "eu", "asia",
                  "ca")
                enum:
                - com
                - eu
                - asia
                - ca
                type: string
            required:
            - environmentId
            - region
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
